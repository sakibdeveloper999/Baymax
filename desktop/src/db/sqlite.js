/**
 * SQLite Module (deprecated - using IndexedDB instead)
 * Kept for backward compatibility
 * 
 * Replaced by: src/db/localdb.js (IndexedDB)
 * Reason: better-sqlite3 has native compilation issues in Electron
 *         IndexedDB works natively in Electron without build dependencies
 */

export default {};
